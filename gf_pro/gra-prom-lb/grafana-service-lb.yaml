apiVersion: v1
kind: Service
metadata:
  name: nginx-loadbalancer-grafana
  namespace: monitoring
  labels:
    app: grafana
spec:
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
      protocol: TCP
      name: http
  selector:
    app: grafana